# SQL_inj_on_Mission_Systems
The model acts as an intelligent real-time firewall layer at the database interface of Satellite ground-station systems which handels telemetry, command logs, orbit  parameters, and mission-critical sensor data. 
